# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:
  - type: dnf
    install:
      skip-unavailable: true
      allow-erasing: true
      packages:
        - "@virtualization"
        - gnome-boxes
      exclude:
        - virt-viewer

  - type: systemd
    system:
      enabled:
        - libvirtd.service
        - virtlogd.service

  - type: kargs
    kargs:
      - kvm_amd.nested=1
      # - iommu=pt
      # - amd_iommu=on
