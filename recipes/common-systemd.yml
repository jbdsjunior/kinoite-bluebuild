---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:
  - type: systemd
    user:
      enabled:
        - topgrade-boot-update.timer
        - topgrade-system-update.timer
        - topgrade-flatpak-update.timer
