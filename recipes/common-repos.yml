# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:
  - type: bling
    install:
      - negativo17

  - type: rpm-ostree
    repos:
      - terra.repo
      - brave-browser.repo
      - google-chrome.repo
      - microsoft-edge.repo
      - vscode.repo
