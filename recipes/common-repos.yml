# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:
  - type: bling
    install:
      - rpmfusion

  - type: rpm-ostree
    repos:
      - terra.repo
      # - brave-browser.repo
      # - google-chrome.repo
      # - vscode.repo
