# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:
  - type: dnf
    repos:
      nonfree: rpmfusion
      cleanup: true
    install:
      allow-erasing: true
      skip-unavailable: true
      packages:
        - mesa-va-drivers-freeworld  # bling rpmfusion
        - mesa-vulkan-drivers-freeworld  # bling rpmfusion

  - type: dnf
    repos:
      nonfree: rpmfusion
      cleanup: true
    install:
      allow-erasing: true
      skip-unavailable: true
      packages:
        - ffmpeg
        - ffmpeg-libs

        # - libva # is already installed
        - libva-utils

        # - gstreamer1-plugin-libav # is already installed
        - gstreamer1-plugins-bad-freeworld
        - gstreamer1-plugins-bad-free-extras
        - gstreamer1-plugins-ugly
        - gstreamer1-vaapi

  - type: dnf
    install:
      allow-erasing: true
      skip-unavailable: true
      packages:
        - pipewire-codec-aptx          # Codec aptX para áudio Bluetooth de alta qualidade
        # - lame                         # Codificador MP3 # is already installed
        - twolame                      # Codificador MPEG-2
        - vorbis-tools                 # Ferramentas Ogg Vorbis

  # - type: dnf
  #   install:
  #     allow-erasing: true
  #     skip-unavailable: true
  #     packages:
  #       - "@multimedia"
  #       - "@sound-and-video"

  # - type: dnf
  #   install:
  #     allow-erasing: true
  #     skip-unavailable: true
  #     packages:
  #       - rocm-smi
  #       - rocm-hip
  #       - rocm-opencl
  #       - rocm-runtime
  #       - rocblas
  #       - rocm-clinfo
