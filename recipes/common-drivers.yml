# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:
  - type: dnf
    install:
      allow-erasing: true
      skip-unavailable: true
      packages:
        - "@multimedia"
        - "@sound-and-video"
        # # Hardware acceleration (Primary AMD RX 6600 XT)
        - mesa-va-drivers-freeworld
        - mesa-vulkan-drivers-freeworld
        - mesa-vdpau-drivers-freeworld
        # AMD observability and diagnostics
        - rocm-smi

        # ROCm/HIP userspace stack for local LLM runtimes (Ollama/LM Studio)
        - rocm-hip
        - rocm-opencl
        - rocm-runtime
        - rocblas
        - rocm-clinfo

        # Full multimedia framework and FFmpeg integration
        - ffmpeg
        - ffmpeg-libs

        - gstreamer1-plugins-bad-freeworld
        - gstreamer1-plugins-ugly
        # - gstreamer1-libav ## already installed

        - libavcodec-freeworld

        # Image format support (HEIC/HEVC/AVIF)
        - heif-pixbuf-loader
        # - libheif ## already installed
        - libheif-tools

        # High-definition Bluetooth audio (AAC, LDAC)
        - fdk-aac

        - x264-libs
        - x265-libs
