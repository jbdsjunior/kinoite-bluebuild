# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:
  - type: dnf
    repos:
      cleanup: true
      copr:
        - lilay/topgrade
        - atim/starship
    install:
      skip-unavailable: true
      allow-erasing: true
      packages:
        - topgrade
        - starship

  - type: dnf
    install:
      skip-unavailable: true
      allow-erasing: true
      packages:
        - git
        - rclone

  - type: dnf
    install:
      skip-unavailable: true
      allow-erasing: true
      packages:
        - distrobox
        - podman-compose
        - podman-docker
        - podman-machine

  - type: dnf
    install:
      skip-unavailable: true
      allow-erasing: true
      packages:
        - fastfetch
        - tailscale
        - fuse
        - fuse-libs
        - fuse3
