# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:
  - type: kargs
    kargs:
      - mitigations=auto
      - amd_pstate=active
      - amd_iommu=on
      - pcie_aspm=performance
