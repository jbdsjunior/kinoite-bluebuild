# Create the system VM images directory
d /var/lib/libvirt/images 0755 root root -

# Apply No_COW (+C) for better VM image performance on BTRFS
# "h" means file attributes (chattr)
h /var/lib/libvirt/images - - - - +C
