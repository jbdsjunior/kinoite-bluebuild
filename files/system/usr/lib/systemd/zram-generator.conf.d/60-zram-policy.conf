# 64 GB workstation policy: keep compressed swap available for pressure spikes without
# dedicating excessive CPU/RAM to a large compressed device during normal desktop use.
[zram0]
zram-size = min(ram / 4, 8192)
compression-algorithm = zstd
swap-priority = 100
