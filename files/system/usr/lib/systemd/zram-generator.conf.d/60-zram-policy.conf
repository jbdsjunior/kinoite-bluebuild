[zram0]
# Scale ZRAM to 50% of RAM with 32GB cap for high-memory workstations (64GB+)
# Previous 16GB cap was insufficient for LLM/AI workloads on 64GB systems
zram-size = min(ram * 0.5, 32768)
compression-algorithm = zstd
swap-priority = 32767
fs-type = swap
