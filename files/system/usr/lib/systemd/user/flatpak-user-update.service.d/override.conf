[Unit]
Wants=network-online.target
After=network-online.target

[Service]
# Fail fast without network and prefer idle runtime behavior.
ExecStartPre=/usr/bin/nm-online -q --timeout=60
Nice=19
IOSchedulingClass=idle
